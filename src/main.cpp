#define GLEW_STATIC
#include <GL/glew.h>
#include <GLFW/glfw3.h>
#include <GLFW/glfw3.h>


int main()
{
  // main window
  GLFWwindow* win = initGraphics();

  // main graphics loop
  while(!glfwWindowShouldClose(win))
  {

  }
}
